import * as PIXI from 'pixi.js';

export class GameObj{

    x: number;
    y: number;
    vx: number;
    vy: number;
    value: number;
    isDestroyed: boolean;
    ObjTag: boolean

    constructor(yPos: number, Speed:number){
    
    };
    update(dt)
    {};
}